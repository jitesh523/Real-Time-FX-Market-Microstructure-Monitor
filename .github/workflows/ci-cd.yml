name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 0'  # Weekly security scan

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov
    
    - name: Run tests
      run: |
        pytest tests/unit/ tests/integration/ -v --cov=src --cov-report=xml --cov-report=html
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install black pylint mypy isort
    
    - name: Run black
      run: black --check src/
    
    - name: Run isort
      run: isort --check-only --profile black src/
    
    - name: Run pylint
      run: pylint src/ --disable=all --enable=E --disable=E0401
    
    - name: Run mypy
      run: mypy src/ --ignore-missing-imports --no-strict-optional --disable-error-code=var-annotated --disable-error-code=import-untyped

  security:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Run Bandit security scan
      run: |
        pip install bandit
        bandit -r src/ -f json -o bandit-report.json || true
    
    - name: Upload security report
      uses: actions/upload-artifact@v4
      with:
        name: bandit-report
        path: bandit-report.json

  build:
    runs-on: ubuntu-latest
    needs: [test, lint]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Docker image
      run: docker build -t fx-monitor:${{ github.sha }} .
    
    - name: Log in to Docker Hub
      if: github.ref == 'refs/heads/main' && secrets.DOCKER_USERNAME != ''
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Push to Docker Hub
      if: github.ref == 'refs/heads/main' && secrets.DOCKER_USERNAME != ''
      run: |
        docker tag fx-monitor:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/fx-monitor:latest
        docker tag fx-monitor:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/fx-monitor:${{ github.sha }}
        docker push ${{ secrets.DOCKER_USERNAME }}/fx-monitor:latest
        docker push ${{ secrets.DOCKER_USERNAME }}/fx-monitor:${{ github.sha }}
